{
  "name": "@noname/cli",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "bin": {
    "noname": "./dist/index.js"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "biome lint",
    "format": "biome format --write",
    "test": "vitest run --passWithNoTests",
    "prepublish": "npm run build"
  },
  "dependencies": {
    "commander": "^15.0.0",
    "execa": "^9.0.0"
  },
  "devDependencies": {
    "typescript": "^7.0.0",
    "tsx": "^4.0.0",
    "@types/node": "^26.0.0"
  }
}
